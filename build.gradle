plugins {
    id 'net.neoforged.moddev' version '2.0.140' apply false

}

// This tells the root to let the subpacks use the NeoForge plugin
subprojects {
    apply plugin: 'net.neoforged.moddev'
    apply plugin: 'java-library'
    apply plugin: 'idea'
}
